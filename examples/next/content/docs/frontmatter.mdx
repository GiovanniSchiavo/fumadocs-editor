---
title: Frontmatter Fields
description: Structured metadata on top of the page
icon: Rocket
---

## Frontmatter

This page exercises the fields Fumadocs reads from the YAML frontmatter:
`title`, `description` and `icon`.

A future editor version could expose them as structured fields while keeping
the body as MDX source.
